[package]
name = "coven-rover"
version = "0.1.0"
edition = "2021"
authors = ["Alexander Shultis <shultis@hawaii.edu>"]
description = "Lightweight COVEN rover daemon - no ROS2"
license = "MIT"

[dependencies]
# GPIO and hardware
rppal = "0.19"              # Raspberry Pi GPIO, PWM, I2C, SPI

# Async runtime (only features we actually use — "full" caused OOM during compilation)
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync", "io-util", "signal"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Serial port (for YDLiDAR)
tokio-serial = "5.4"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Error handling
anyhow = "1"

# Config
toml = "0.8"
shellexpand = "3"

[profile.release]
opt-level = "z"     # Optimize for size
lto = true          # Link-time optimization
codegen-units = 1   # Single codegen unit for better optimization
strip = true        # Strip symbols

[profile.dev]
opt-level = 1       # Some optimization even in dev (for timing)

[profile.test]
inherits = "dev"
opt-level = 0       # Skip LLVM optimizer for tests — prevents OOM on 15GB systems

# Cross-compilation targets
# Build with: cargo build --release --target arm-unknown-linux-gnueabihf
